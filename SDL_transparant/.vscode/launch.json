{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug SDL3 App",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/sdl3_transparent_window",
            "args": ["${workspaceFolder}/test.png"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/build",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "build"
        }
    ]
}
